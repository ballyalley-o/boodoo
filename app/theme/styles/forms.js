import classNames from 'classnames'

export const StyledInputField = classNames(
  'bg-gray-50 border border-gray-600 text-gray-900 sm:text-sm rounded-lg focus:ring-blue-600 focus:border-gray-200 w-full p-2.5'
)

export const StyledInputLabel = classNames(
  'block mb-2 text-sm font-medium text-gray-900'
)
